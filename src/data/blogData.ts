
import type { BlogPost } from '@/types';

// This file is now a fallback and for type reference.
// The primary source of truth for blog data is now Firestore.
export const blogData: BlogPost[] = [];
